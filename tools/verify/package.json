{
  "name": "@dogfood-labs/verify",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Central verifier for dogfood-labs. Validates submissions against schema and policy, produces persisted records.",
  "main": "index.js",
  "scripts": {
    "test": "node --test",
    "verify": "node cli.js"
  },
  "dependencies": {
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0"
  }
}
